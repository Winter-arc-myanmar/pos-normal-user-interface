# Shared UI

Reusable presentational pieces that are not feature-specific.

Prefer:

- `src/shared/ui` for generic UI atoms
- `src/components/ui` for existing shared controls already used by pages

Domain logic belongs in `src/core`, not here.
See root [architecture.md](../../architecture.md).
