# i18n

Locale files live in `locales/`.

- `en.json` is the source of truth for template keys.
- `my.json` overrides English with Burmese. Missing keys fall back to English.

Supported languages: English (`en`) and Burmese (`my`).
